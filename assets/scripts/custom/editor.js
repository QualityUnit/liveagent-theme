// window.addEventListener( 'load', () => {
// 	setTimeout( () => {
// 		const metaboxes = document.querySelector( '.edit-post-layout__metaboxes' );
// 		const editHeader = document.querySelector( '.edit-post-header' );
// 		const showMetaboxButton = document.createElement( 'button' );
// 		showMetaboxButton.classList.add( 'showMetaboxes' );
// 		showMetaboxButton.innerText = 'Show/hide Custom Fields';
//
// 		if ( metaboxes ) {
// 			editHeader.insertAdjacentElement( 'afterbegin', showMetaboxButton );
// 			showMetaboxButton.addEventListener( 'click', ( ) => {
// 				metaboxes.classList.toggle( 'visible' );
// 			} );
// 		}
// 	}, 0 );
// } );
